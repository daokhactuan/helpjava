pipeline {
    agent any
    stages {
        stage('Build') { 
            steps {
                sh 'pwd'
//                sh '/opt/apache-maven-3.8.1/bin/mvn -B -DskipTests clean package' 
            }
        }
    }
}
